<?php
// Copie ce fichier en "config.php" (à côté, même dossier) et remplis les
// valeurs — config.php n'est jamais commité dans Git (voir .gitignore),
// donc les identifiants de la base ne quittent jamais ton serveur OVH.

// Base de données : identifiants visibles dans le manager OVH ->
// "Bases de données" -> ta base -> "Se connecter" (ou "Informations").
define('DB_HOST', 'xxxxxx.mysql.db'); // ex. celui fourni par OVH
define('DB_NAME', 'xxxxxx');
define('DB_USER', 'xxxxxx');
define('DB_PASS', 'xxxxxx');

// Jeton à usage unique pour créer le tout premier compte enseignante via
// setup.php (voir server/README.md). Choisis une longue chaîne aléatoire
// toi-même (ex. génère-la avec `openssl rand -hex 32` ou un gestionnaire de
// mots de passe) — ne me la communique jamais, elle ne sert qu'entre ton
// navigateur et ton serveur.
define('SETUP_TOKEN', 'xxxxxx');

// Clé API Google Cloud Text-to-Speech (restreinte à cette seule API dans la
// console Google Cloud : "API et services" -> "Identifiants" -> "Créer des
// identifiants" -> "Clé API", puis "Restreindre la clé" -> API Cloud
// Text-to-Speech uniquement). Sert à générer automatiquement le mp3 d'un mot
// ajouté par l'enseignant (voir lexicon.php et tts.php) — distincte de la clé
// de compte de service utilisée pour la génération en masse initiale
// (scripts/generate-word-audio.mjs), une clé API simple suffit ici pour un
// appel REST direct depuis PHP. Laisse vide ('') pour désactiver : le mot
// reste ajoutable, seule la prononciation retombera sur la synthèse du
// navigateur (comportement déjà prévu côté client).
define('GOOGLE_TTS_API_KEY', '');

// Sauvegarde automatique quotidienne de la base (voir server/backup-db.php
// et server/README.md "Sauvegardes") vers Backblaze B2 (10 Go gratuits,
// largement suffisant ici). Crée un compte sur backblaze.com, un bucket
// PRIVÉ, puis "App Keys" -> "Add a New Application Key" (restreinte à ce
// bucket si possible) pour obtenir keyID et applicationKey.
define('B2_KEY_ID', 'xxxxxx');
define('B2_APPLICATION_KEY', 'xxxxxx');
define('B2_BUCKET_NAME', 'xxxxxx'); // le nom exact du bucket créé sur B2
